// 5: max - Write a function with two number arguments. Have it return the largest number of the two.
const max = (number1, number2) => {
  return number1 > number2 ? number1 : number2;
};

const result = max(78, 98);

console.log(result);
